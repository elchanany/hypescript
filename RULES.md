# RULES.md — חוקים מחייבים

> חוקי עבודה שאסור להפר. אלה לא המלצות. כל סוכן שעובד בפרויקט מחויב להם.
> החלטה שסותרת חוק כאן — עוצרים ושואלים את המשתמש לפני שממשיכים.

## 1. פרטיות — הווידאו לא עוזב את המחשב
עיבוד הווידאו חייב להישאר בצד-לקוח (בדפדפן דרך `ffmpeg.wasm`) או מקומי (`local/`).
**אסור** להעלות קובצי וידאו לשרת/אחסון ענן. היחיד שמותר לשלוח החוצה הוא **האודיו
הדחוס** לצורך תמלול (Groq/OpenAI), ורק דרך המנגנון הקיים.

## 2. חינמי ו-open-source בלבד
אין להוסיף תלות בשירות בתשלום או ב-API סגור, מעבר למפתחות ה-API של המשתמש עצמו
(שהוא מזין בעצמו). אין להטמיע מפתחות בקוד.

## 3. סנכרון ליבה בין שתי האפליקציות
לוגיקת הליבה משוכפלת: `web/lib/` (TypeScript) ו-`local/hypescript/` (Python).
**כל שינוי התנהגותי** באלגוריתם (יישור-סקריפט, בניית קטעי keep, הסרת מהססים, מיפוי
ציר-זמן, בניית SRT) חייב להתעדכן **בשני הצדדים**, אחרת התוצאות יסטו. ליבה שנבדקה:
התוצאות של שתי הגרסאות זהות — שמור על כך.

## 4. הפרדת אפליקציות
אין לערבב קוד בין `web/` ל-`local/`. אין ל-`web` לייבא מ-`local` או להפך. תשתית
משותפת קונספטואלית — כן; ייבוא קוד ישיר — לא.

## 5. סוכן AI — מחוץ לטווח עד אישור
אין לבנות את סוכן ה-AI (חיתוך/עריכה אוטונומית) עד אישור מפורש של המשתמש. מותר
placeholder בהגדרות בלבד. ראה [ROADMAP.md](ROADMAP.md).

## 6. עברית ו-RTL
כל פלט טקסטואלי למשתמש (כתוביות, ממשק, לוגים גלויים) חייב להיות עברית תקינה עם RTL
נכון: UTF-8, וסימון כיווניות (U+200F) בכתוביות כדי שפיסוק/מספרים לא יתהפכו.

## 7. אין מערכות לא-מאושרות
אין להוסיף frameworks, שכבות abstraction, מסדי נתונים או שירותים חדשים בלי אישור.
כשמתלבטים בין הרחבה לפתרון פשוט — בוחרים בפשוט ומציעים למשתמש.

## 8. תיעוד החלטות
החלטה ארכיטקטונית משמעותית מתועדת בקובץ הרלוונטי ([ARCHITECTURE.md](ARCHITECTURE.md)
או [STACK.md](STACK.md)). "טרם הוחלט" נכתב במפורש — לא ממלאים בניחוש.
