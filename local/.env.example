# =============================================================================
# hypescript local (Python CLI/GUI) — תבנית מפתחות
# =============================================================================
# העתק ל: local/.env   ואז מלא ערכים.
#   cp .env.example .env
# =============================================================================

# תמלול בענן (אופציונלי אם משתמשים במנוע מקומי)
# https://console.groq.com/keys
GROQ_API_KEY=

# חלופה:
# OPENAI_API_KEY=
