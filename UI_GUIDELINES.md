# UI_GUIDELINES.md — עקרונות ממשק

> עקרונות לממשק אפליקציית ה-web. משקף את מה שקיים; אין לפרש מוקדם מדי מעבר לזה.
> חל על `web/` (יש UI). ל-`local` יש GUI פונקציונלי ב-Tkinter — לא נדרש עיצוב מוקפד שם.

## עקרונות

- **RTL מלא.** `<html dir="rtl">`. טקסט, כותרות ותוויות מיושרים לימין. אזורי לוג
  טכניים (פלט מונוספייס) — LTR, זו פשרה מכוונת לקריאוּת של נתיבים/מספרים.
- **פשטות מעל הכל.** זרימה של שלושה שלבים: קבצים → עריכה → תוצאות. בלי לוחות בקרה
  עמוסים. פעולה ראשית אחת בולטת בכל שלב.
- **שקיפות לפני פעולה.** לפני רינדור מציגים מה ייחתך (תמלול-סופי, סטטיסטיקות, מספר
  חיתוכים). "בדיקה מהירה" תמיד זמינה לפני הפעולה הכבדה.

## שפה חזותית (קיים)

- ערכת צבעים כהה, מוגדרת כ-CSS variables ב-`web/app/globals.css` (`--bg`, `--card`,
  `--accent` וכו'). שינויים גלובליים — דרך המשתנים, לא ערכים קשיחים.
- כרטיסים (`.card`) לקיבוץ תוכן; גבולות עדינים; פינות מעוגלות.
- כפתור ראשי אחד בולט (`.btn.primary`), פעולות משניות שקטות (`.btn`).
- גופן: Segoe UI / system.

## נגישות
- ניגודיות מספקת בין טקסט לרקע (הערכה כהה עומדת בכך).
- שדות עם `<label>` ברור; קלט מקלדת עובד; states של focus גלויים.

## מה שטרם הוחלט
- לוגו/מיתוג של העמותה (הוצע — לא נבחר).
- מצב בהיר (כרגע כהה בלבד).
- תצוגה מקדימה מוטמעת של הווידאו הערוך (כרגע הורדה בלבד).
