# User-Reported Blockers

Every issue the product owner reported, tracked until **verified in the running product** —
not closed because "something similar" was changed.

**Status values:** `open` · `investigating` · `root-caused` · `fixed-unverified` · `verified` · `deferred-by-owner`

> Rule: `verified` means it was exercised in the real app (or by a real render/API call), not that
> a build passed. `deferred-by-owner` is only for the two things the owner explicitly postponed:
> full integration of providers with no keys yet, and marketing assets in languages other than Hebrew.

---

## P0 — Editor core (product is not usable without these)

| ID | Issue | Status | Root cause / notes |
|----|-------|--------|--------------------|
| B-38 | **Two shipped features were dead in production because their migration was never applied** | **fixed + guarded** | Not user-reported — found while closing B-25, then confirmed as a pattern. The repo held 11 migration files; the production database had applied 9 of them. The two missing ones were the two newest feature tables, `user_provider_secrets` (BYOK keys) and `user_brand_kits`. Both features therefore failed on **every** call, and both failed *quietly*: Brand Kit swallowed the error in an empty `catch` and rendered as "no kit yet", BYOK returned a 503 whose message meant nothing to the user. Nothing in the build, the test suite, or the type checker can see this — a missing table is a runtime fact about a remote system, and `tsc` is perfectly happy with `.from("table_that_does_not_exist")`. Both migrations are now applied. The guard against recurrence is `scripts/check-cloud-schema.mjs`: it derives the required table list **from the source itself** (every `.from("…")` in `web/app` and `web/lib`, so a new table is covered the moment it is written, with no list to maintain), asks PostgREST whether each relation exists, and additionally checks that secrets the code has a minimum length for actually meet it — which is how the 13-character `BYOK_ENCRYPTION_KEY` surfaced. Verified in both directions: 13/13 tables pass now, and the old name `user_profiles` returns `404 Could not find the table`, so the check genuinely detects absence rather than always passing. Run it against production env before a deploy. |
| B-37 | Editor came up **empty although the project was saved** | **not a product bug — dev artifact** | Seen twice while working: the timeline and media library rendered empty even though IndexedDB still held the full project (clips, 4 tracks, and the 875,763-byte mp4 blob — all read back and verified). Cause: `npm run build` was run against the same `.next` directory a live `next dev` server was serving, so the browser received mismatched chunks (404s for `main-app.js` / `app/page.js`, and a production chunk name in a dev session) and the client bundle never hydrated. Nothing was lost. Could **not** be reproduced on a clean dev server: upload → drag → dismiss consent → skip tour → reload all restored correctly, and the clip stayed put through 10s of polling. `scripts/agent-build.mjs` already exists for exactly this reason (isolated `distDir` per build) and should be used instead of `npm run build` while a preview server is up. |
| B-36 | **Editor rendered a blank white page** (whole app dead) | **fixed, verified in browser** | `useEditor` returned an `addOverlay` that was never defined in the hook, so building its return object threw `ReferenceError` on every render and the entire EditorPage unmounted into the error boundary. A partial edit had been left in the working tree without ever running `tsc` (which does catch it — TS18004, confirmed with a probe). Before: `document.body` had 0 characters. After: editor root, top bar, tool rail and timeline all render. |
| B-01 | Drag a clip on the timeline → it moves, but **snaps back on release** | **verified in browser, incl. reload** | Root cause was B-02 (the vacated space collapsing). The full acceptance test now passes end to end, measured in the running app: the clip was dragged onto the "new track" drop zone (which appears at 42px during the drag), released, and a second video track *וידאו 2* was created with the clip at 5.4s and a 5.4s gap before it. After a full page reload the tracks, the gap and the clip position (`left: 40.12%`) were all still there. |
| B-02 | **Free placement** — must be able to drop at any time (00:03.200), leave gaps, not forced adjacent | **verified in browser** | Root cause proven by probe: on the PRIMARY video track `moveClipAtTimeline` filtered the clip out instead of leaving a gap, so every later clip slid left (A[0-2] B[2-4] C[4-6] → moving B to 10s moved C from 4s to 2s). Now the vacated space becomes a gap on every track. 5 regression tests in `lib/editor/freePlacement.test.ts`. Confirmed live: dragging the only clip right produced a `רווח 3.0s` gap clip and the Inspector read `מיקום בציר 00:02;29`; the gap survived a reload. |
| B-03 | **Magnetic snapping** with vertical guide; snapped timestamps must be **exactly** equal | **fixed** | Snapping, guides and an Alt bypass already existed and the radius was already pixel-derived — but it was clamped with `Math.max(0.04, …)`, and 0.04s is longer than a frame at 30fps, so zooming in for frame-accurate work could not defeat the magnet. Now `snapToleranceSec()` (pure, 6 tests) with `SNAP_PIXELS = 10` and no seconds floor. |
| B-04 | **Audio-only files must not create a visual layer** (mp3 showing as video rectangle/thumbnail) | **verified (logic)** | Verified: both add paths route `kind === "audio"` to the audio track, and `usableClips` excludes audio from the visual set, so it cannot cover the video. Locked by `lib/render/audioOnly.test.ts` (4 tests). |
| B-05 | **Overlap is a feature** — multiple video/image/text/audio at the same time on different tracks | **partly fixed** | Measured what already works: overlapping video tracks resolve by occlusion (`flattenVideoTracks` picks the topmost track per time slice), the audio lane mixes in parallel (`adelay`+`amix` in `graph.ts`), and overlays composite on top post-concat. What was **broken**: mute. Export and preview both read `audioMuted(tracks)` — the *audio track's* flag — and applied it as a global gain multiplier, so muting the music silenced the speech in the video, while muting a video track did nothing at all. Now `applyTrackMute()` resolves mute per clip from its own track, in the preview, the export and the agent's export alike. 8 tests in `lib/editor/trackMute.test.ts`, two of which assert the gain in the real FFmpeg filter string. Still open: audio of a video-track clip that an upper track occludes is dropped rather than mixed. |
| B-06 | **Z-order / layer reorder** consistent between Canvas, Timeline, Inspector and Export | **fixed** | Single convention in `lib/editor/layerStack.ts`: higher `zIndex` = closer to the viewer, ascending sort everywhere (preview render, `materializeOverlays` export render). Four CommandBus commands (`overlay.bringToFront/bringForward/sendBackward/sendToBack`) replace the old Inspector `zIndex±1`, which could collide with a neighbour; `reorderOverlayZ` renumbers to a gap-free 0..n-1 sequence and self-heals existing collisions. User-facing numbering is `layerDepth()` (1 = topmost, as requested) and both the canvas badge and the Inspector read it, so they cannot disagree. 26 tests in `layerStack.test.ts`. |
| B-07 | **Select a layer behind another layer** on the canvas (Alt+Click cycling / layer picker) | **fixed** | Selection was `document.elementsFromPoint`, which cannot see a box whose pointer-events are off. Now geometry-based (`hitTestOverlayStack`, rotation-aware, honours hidden/locked and the current time), with `cycleLayerSelection` on Alt+Click walking one step down the stack and wrapping. Fixed a second bug on the way: a plain click used to snap the selection back to the topmost layer, so a layer reached with Alt+Click could never be dragged — a plain click now keeps manipulating the already-selected layer when it is under the cursor. Hover draws a dashed outline and never changes the selection. |
| B-08 | **Playhead hard to drag** at some zoom levels | **fixed, measured in browser** | Measured with `elementFromPoint`: away from the lane edge the playhead already had a 14px grab area, but **at the start of the timeline only 7px** — the left half fell under the sticky track-header column (`.tl-head2`, z-index 12 against the playhead's 8), so half the target was unreachable exactly where the playhead sits by default. Raising the playhead above the header would make it paint over the track names while scrolling, so instead `at-start` shifts the same grab width entirely inside the lane without moving the line. Re-measured: 15px at t=0, 16px elsewhere, and a drag from t=0 to mid-lane landed on `0:04.0` of an 8s clip. |
| B-09 | **Ruler ticks disappear** at low zoom | open | Needs adaptive tick density (frames → sec → 5s → 30s → 1m → 5m). |
| B-10 | Linked video/audio must **not** behave as multi-selection | open | Link ≠ selection; split/trim/move affect both only when Linked Selection is ON. |

## P1 — Interaction & UX

| ID | Issue | Status | Notes |
|----|-------|--------|-------|
| B-11 | Chat bubbles: user vs agent must point opposite directions, RTL correct, minimal (not childish) | open | |
| B-12 | Composer collisions — `+`, `@`, attach, stop, send overlap the text | **fixed, measured in browser** | The reserved zones existed but the numbers did not add up: `box-sizing: border-box` with `padding: 44px … 40px` (84px) against a box height of 82px left the typing area **0 pixels tall** — measured `contentH: 0`, `scrollHeight 104 > clientHeight 84`, i.e. the text was clipped behind the buttons. `lib/ui/composeBox.ts` is now the single source for the geometry (shared with the CSS), the stored height is re-clamped so an old saved value cannot resurrect the broken box, and 10 tests assert that even the minimum height leaves a full writable line. Re-measured: `contentH 63` (3 lines), `scrollHeight == clientHeight`, zero button/text overlaps. |
| B-13 | Stop button overlaps composer while the agent works | **fixed, measured in browser** | While the agent runs the bottom row holds *two* buttons (send + stop) and is 67px wide, against a reserved gutter of only 56px — so text ran under the stop button. `.chat-compose.running` now widens the reserved gutter to 83px. Measured in both states: `overlaps: []`. |
| B-14 | **Steering queue** — send guidance while the agent works; queued items listed compactly with "push now" | **implemented** | `steering-queue-dock` above the composer: numbered list in order, per-item status (`queued` / applied), a "החל עכשיו" button that injects one note into the running agent mid-work, per-item removal and "נקה הכל". Backed by `AgentRunner.injectMessage`, which the loop reads at the start of the next iteration. |
| B-15 | Frame/screenshot attachments in chat are **cropped/clipped** | **fixed** | Aspect ratio was already preserved (`object-fit: contain`), so nothing was actually being cropped — the real problem is that the card caps media at 200px, which makes a full-resolution export frame too small to inspect. Clicking a frame now opens it full size (up to 96vw / 92vh), closing on Escape, on the backdrop, or on the close button. Uses the new `--z-modal` token rather than another ad-hoc number. |
| B-16 | Agent output media (image/audio/video) needs real previews + players + "add to timeline" | **fixed** | `ChatMediaCard` already had real players — a video player with play/seek/mute/fullscreen and a timecode, a waveform-style audio player, an SRT preview, and a download with a safe filename. The missing half was getting the result back into the project: every generated video, image and audio card now has "הוסף לפרויקט", which fetches the blob and routes it through the same media pipeline as a normal upload, showing a spinner and then "נוסף לפרויקט". |
| B-17 | Upload progress widget is off to the side; should be centred under the player | open | |
| B-18 | Clear **back-to-projects** button in the editor | likely-fixed | TopBar now has two dashboard links; needs visual verification. |
| B-19 | App-level **z-index architecture** (dropdown behind preview, etc.) | **fixed, measured in browser** | There were ~70 z-index values scattered between 1 and 10000, and they contradicted each other in ways that produced real bugs: the aspect-ratio dropdown sat at `9999 !important` and floated over open dialogs *and* over the cookie banner; toasts sat at 100 and therefore vanished under the export dialog (900), the admin dialog (1000) and the shortcuts dialog (9999); the onboarding tour at 10000 covered the legally-required consent banner at 5000; and the timeline's drag ghost at 10000 floated over modals. There are now nine documented tokens on `:root` and every floating layer uses one. Measured live: all five dialog variants resolve to 800, toast 900, popovers 300, context menu 400 (backdrop 399), tour 1000, consent 1100, timeline floats 200. Component-local values (1-15 inside the timeline or a card) were deliberately left alone — they are local stacking, not part of the ladder. |

## P2 — Platform / SaaS

| ID | Issue | Status | Root cause / notes |
|----|-------|--------|--------------------|
| B-20 | **ElevenLabs "not working"** → inaccurate transcription | **root-caused** | Key is **valid**; Hebrew STT verified working (scribe_v1 + scribe_v2, HTTP 200, `heb` @ p=1.0, word timestamps). The account is **free tier at 7,249/10,000 chars (72%)**. On exhaustion the route silently falls back to Groq with `quality: reduced` — that is the "inaccurate transcription". **Owner action required: upgrade the ElevenLabs plan.** |
| B-21 | No way to tell a working provider from a configured-but-broken one | **fixed-unverified** | Added live probes (`lib/providers/probe.server.ts`), `unhealthy` status, admin endpoint `/api/providers/health`, and CLI `node scripts/check-providers.mjs`. Verified locally against the live ElevenLabs API. |
| B-22 | **413 Request Entity Too Large** | **fixed** | **Not** media upload (presigned straight to R2, 5 GB cap) and **not** transcription (chunked to 120 s ≈ 0.72 MB). Chat attachments were also cleared: they go through the normal media pipeline and only an `@media:` tag reaches the request. The real remaining path is `/api/agent`, which resends the **entire** history every turn — and once it crossed 4.5 MB, "try again" resent the identical body and failed identically, so the conversation died with no way back. `lib/agent/requestBudget.ts` now enforces the budget *before* sending: oldest turns are dropped first, system messages and the recent tail are protected, and one system note tells the model that history was truncated. A 413 that still happens gets a Hebrew explanation instead of `FUNCTION_PAYLOAD_TOO_LARGE`. 12 tests, including one that drives a real `AgentRunner` with a ~12 MB history and asserts the body actually sent is under the platform limit and still contains the newest user message. |
| B-23 | Cloud must be the **default**: files in cloud, cross-device login, edits/chats saved, **fast cloud render** | **fixed — worker deployed** | Full audit in `docs/CLOUD_SAAS_AUDIT.md`. Persistence, cross-device and the cloud render pipeline (worker, queue, callbacks, quotas, all four env vars in production) already work. The "half an hour for a five-minute video" has a single named cause: `cloudCapable` included `!(burnCaptions && subs?.length)`, and `burnCaptions` defaults to **on** — so every project with subtitles, which for a captioned-lecture editor is essentially every project, silently fell back to single-threaded `ffmpeg.wasm` in the browser. **And the fallback lied**: any cloud failure was `console.warn` + `blob = null`, while the export dialog kept saying "מייצאים את הסרטון בענן" and "הרינדור מתבצע על שרתי הענן של Google". Now `lib/render/renderRoute.ts` decides and explains in one pure place: the dialog title, the note, and the completion toast all state where the work actually ran, and each fallback names its own reason (upload still running / media not in cloud / text overlay / captions / cloud unavailable / quota exhausted) — a quota refusal is no longer indistinguishable from an outage. 9 tests, one of which asserts the word "בענן" can never appear in the device-render title. The actual speed fix — teaching the worker to burn captions, gated behind a capability flag so nothing breaks before it is deployed — is now **live**: revision `hypescript-render-00005-x2h` in `me-west1` serves 100% of traffic and `/health` returns `{"subtitles":true,"imageOverlays":true,"textOverlays":false,"audioMix":true}`, so captioned projects route to the cloud instead of falling back to the browser. Still unverified end-to-end: Hebrew glyphs on the *deployed* image (libass + Noto Hebrew are in the Dockerfile and pass locally against real FFmpeg, but no lecture has been burned through the deployed revision yet), and `maxScale` is **1**, so renders are serialised across all users. |
| B-24 | Lemon Squeezy still in **test mode**; real subscriptions must work | **code side done — blocked on store configuration** | Test mode was **hard-coded in four separate places**, added while the store was under review and never removed: a live variant was rejected outright, every checkout was created with `test_mode: true`, a real `subscription_created` webhook was rejected with 409, and even a successful row was stamped `lemonsqueezy_test`. The 409 is the dangerous one — money taken, no access granted. All four are now driven by one explicit switch, `BILLING_LIVE_MODE`, enforced **in both directions** so a Preview build can never process real money and a live build can never sell a test product. Also fixed: `getStore()` fell back silently to the first store when `LEMONSQUEEZY_STORE_ID` did not match, which in a multi-store account would have created checkouts against the wrong store — now an explicit `lemon_store_mismatch`. **Default is still test**, so nothing changed in production until the owner flips it. Runbook with the exact order (verify `/api/billing/catalog` readiness → register the live webhook → set its secret → then flip) is in `docs/BILLING_GO_LIVE.md`, including the cleanup of `provider = 'lemonsqueezy_test'` rows that would otherwise block a first real purchase. No purchase was made and no payment setting was touched. **Measured against production afterwards, and the result changes the plan**: `/api/billing/catalog` reports `storeContains: "test-only"`, one product, and all four plan×interval combinations `ready: false` with reason `billing_trial_missing`. Because that reason is *last* in `resolveVariant`'s chain, it proves the four variants already exist with the correct names, intervals and exact prices — the only missing piece is the free trial (`has_free_trial: true` plus `month`×1 or `day`×30). So flipping the switch today would **break** checkout rather than enable it: there is not one live variant to sell. Two dashboard actions are required and cannot be done any other way, because the Lemon Squeezy API is read-only for products and variants — add a one-month trial to the four existing variants (this alone repairs *test* checkout, which is also broken right now), and create the live variants. The post-migration cleanup step turned out to be a no-op: `cloud_subscriptions` holds a single row, `provider = 'admin_override'`, and zero `lemonsqueezy_test` rows. |
| B-25 | Brand Kit must sync to the **cloud**, not local-only | **fixed** | The sync call wrote to `public.user_profiles.brand_kit` — a table that exists in **no migration in the repo**. Every write failed with 500, the caller swallowed it in an empty `catch`, and the GET returned `null` on error too, so a failure was indistinguishable from a new user. Nothing ever called the GET at all, so even a working write would not have restored anything. Now: a real `public.user_brand_kits` table with owner-scoped RLS, sync on **create and update** (not only on activate, which is why editing an active kit never reached the cloud), a read that distinguishes "no kit yet" from "read failed", and an actual restore on a second device when there is nothing local. Also corrected a false privacy claim in the UI: the page said "לא נשלח לענן... ואין סנכרון ענן" while a PUT was already being attempted on every activation — it now states exactly what leaves the device (the text) and what does not (logo and reference images). 6 tests covering the silent-failure paths. The migration is **applied** to the production project (`user_brand_kits`, RLS on, four owner-scoped policies, FK to `auth.users`; the security advisor raises nothing on it). |
| B-26 | BYOK explained gently for non-technical users; upsell not an error message | **backend unblocked; UX still open** | Found while applying B-25: BYOK was not merely unfriendly, it was **non-functional**, and for two independent reasons. (1) `byok.server.ts` reads and writes `public.user_provider_secrets`, a table that existed as a migration file in the repo but had **never been applied** — every save returned 503 `byok_save_failed`. Now applied (RLS on, `revoke all from anon, authenticated`, so only the service role can reach it; the advisor's "RLS enabled, no policy" INFO is the intended design here, not a gap). (2) `BYOK_ENCRYPTION_KEY` is **13 characters** where the code requires ≥32, so `encryptionKey()` throws `byok_encryption_not_configured` and the save fails even with the table present — **now resolved**: Vercel returns `[SENSITIVE]` rather than the stored value, so the production key could not be inspected — instead of guessing, a fresh 61-character key was generated and set on Production and Preview, which is risk-free precisely because `user_provider_secrets` had just been created and held zero rows. The value was piped straight from `openssl` into `vercel env add` and the temp file deleted, so the plaintext never entered a transcript. Production was redeployed (`hypescript-lpnkokgnj`, aliased to `hypescript.vercel.app`) so the new value is actually loaded; `/api/providers/byok` answers 401 rather than 500, i.e. the route is alive and merely wants a signed-in user. Note for the future: rotating this key **after** keys are stored makes every stored key undecryptable. The crypto itself is sound and matches the stated constraints: AES-256-GCM, service-role only, and `listByokProviders` returns provider names only — no key material ever reaches the client. The original request (gentle explanation, upsell that does not read as an error) is still open. |
| B-27 | Error reporting to admin, friendly error text instead of raw HTTP codes | **fixed** | The inventory was extracted mechanically from the source rather than guessed: 70 codes returned by `web/app/api` plus those thrown inside `web/lib`, written to `web/lib/errors/INVENTORY.md`. Researching the domain-specific ones surfaced three the extraction had missed — thrown from a ternary or carrying a dynamic `:suffix` — for 88 entries in `web/lib/errors/messages.ts`. The distinction that does the work is **audience**: `user` when the person can actually act (quota reached, file too large, not signed in, plan does not cover it), `owner` when it is our configuration or infrastructure at fault. Owner-class text never blames the user and never proposes a retry that would fail identically forever; it is also written to the existing `audit_logs` (no new table), so the operator learns from the system rather than from a complaint. `retryable` is reserved for genuinely transient faults, dynamic suffixes resolve by prefix, and an unknown code falls back on the HTTP status instead of rendering `undefined` — the raw code stays on the object so support can still be told which one fired. `worker_cannot_burn_subtitles` is deliberately worded as a notice, not a failure: the export still succeeds in the browser, only slower, and calling that an error would be a lie. Wired only where a user meets an error (central toast, export failure, account, dashboard, preferences), guarded so existing good Hebrew is never re-explained into something generic. 941 tests pass; the guarantee was verified by breaking it on purpose — adding an uncovered code to the inventory fails the suite by name, so a future code without a message cannot reach a user. |
| B-28 | Email system (transactional + lifecycle + unsubscribe + branded RTL) | open | |
| B-29 | Full Hebrew localisation incl. landing assets, correct gender/plural forms | open | i18n system exists; coverage unverified. |

## P3 — Agent brain

| ID | Issue | Status | Notes |
|----|-------|--------|-------|
| B-30 | **Ask / Plan / Act** modes must genuinely change behaviour; Ask blocked from tools **at system level**; **Plan is the default** | **fixed** | Two layers: the request carries no tool schemas in Ask and only read-only schemas in Plan (`PLAN_TOOL_SCHEMAS`), and `modeAllowsTool()` in `AgentRunner.executeTool` blocks the call even if the provider returns one anyway. The block is returned as a *tool result* (so the model stops retrying) and raises `onModeBlocked`, which turns the failed tool card into a mode-switch button instead of a dead "try again". Plan is the default for a new conversation and the mode is stored per conversation, so switching chats cannot leak Act into one still being planned. Proven by `lib/agent/modeEnforcement.test.ts`, which drives a real runner against a provider that *does* ask for `delete_clip` and asserts `run()` is never called. |
| B-31 | **Conversation titles** auto-generated like ChatGPT | **fixed** | One cheap LLM call after the first user message only (`shouldGenerateTitle`), server-side keys, `/api/agent/title`. A manual rename always wins: `titleManual` stops a title request that was still in flight from overwriting the name the user typed, and an auto title does not bump `updatedAt` so it cannot reorder the sidebar. 13 tests in `lib/agent/title.test.ts`. |
| B-32 | **Cross-conversation short-term memory**, summary of last 5 chats, **cached** (no repeated summarisation) | **fixed** | Up to 5 previous conversations in the project are summarised in one call and cached in the chat store under a fingerprint of the sources; a new chat reuses the cache and only re-summarises when a source conversation actually changed. Injected as a system message at the head of the request. 15 tests in `lib/agent/memorySummary.test.ts`, including the "no second call while the cache is valid" case. |
| B-33 | Agent must know every tool/provider it has and choose intelligently (generate vs. import from stock) | open | Stock providers already configured: Pexels, Pixabay, Unsplash, Freesound. |
| B-34 | Agent **self-QA**: render the frame at time T, look at its own work, fix visual problems | open | |
| B-35 | Agent must never silently overwrite a manual user edit (`protectedFromAutomation`) | partial | First instance fixed: an auto-generated conversation title can no longer overwrite a manual rename (`titleManual`). The general `protectedFromAutomation` flag on clips/overlays is still open. |

## Creative library — measured inventory

Real counts, obtained by importing the modules (not by grepping):

| Category | Count | Source |
|---|---|---|
| Video effects | **85** | internal |
| Filters / looks | **65** | internal (FFmpeg-backed) |
| Transitions | **57** | **every FFmpeg `xfade`** minus `squeezev` (segfaults FFmpeg 8.0.1) |
| Clip animations (in/out/combo) | **70** | internal |
| Text animations | 20 | internal |
| Text templates | 50 | internal |
| Caption styles | 23 | internal |
| Shapes | 16 | internal |
| Fonts (curated) | 27 | internal — **being expanded** |
| Motion assets | 9 | internal |
| Stickers (starter) | 12 | internal |
| Icons (curated) | 19 | internal — **being expanded** |

**Keyless expansion verified available (no API key, no cost):**
- **Google Fonts** — `fonts.google.com/metadata/fonts` returns **1,942 families, 62 with the Hebrew subset**.
- **Iconify** — `api.iconify.design/collections` returns **236 sets ≈ 334,395 icons**; licenses vary per set
  (MIT 106, CC BY 4.0 52, Apache 2.0 31, OFL 13, CC BY-SA 4.0 6) and must be tracked per set.
- **GIPHY** genuinely needs a free key (`GIPHY_API_KEY`, not configured) — must show an honest
  "not configured" state, never a fake "connected".
